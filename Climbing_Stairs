
class Solution:
    def climbStairs(self, n: int) -> int:
        lst  = [0,1,2]
        for i in range(3,n+1):
            lst.append((lst[i-1] + lst[i-2]))
        return lst[-1]
    

sol = Solution()
print(sol.climbStairs(n=10))
